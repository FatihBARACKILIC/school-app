import 'package:flutter/material.dart';

Widget user() {
  return Center(
    child: Column(
      children: const [
        Text(
          "User Data",
          textScaleFactor: 5,
        ),
      ],
    ),
  );
}
